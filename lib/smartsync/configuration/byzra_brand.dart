/// Public branding for the reusable synchronization engine.
///
/// Legacy `SmartSync` Dart identifiers and Firestore keys are intentionally
/// retained as compatibility APIs. New user-visible and persisted branding
/// must use this value.
abstract final class ByZraBrand {
  static const String name = 'ByZra';
}
